// Declare different variables
let fruits = ['apple', 'banana', 'mango'];
let name = 'John Doe';
let age = 25;
let numbers = [1, 2, 3, 4, 5];
let isMarried = false;

console.log(
  Array.isArray(fruits) ? 'Fruits is an array' : 'Fruits is not an array'
);
console.log(Array.isArray(name) ? 'name is an array' : 'name is not an array');
console.log(Array.isArray(age) ? 'age is an array' : 'age is not an array');
console.log(Array.isArray(numbers)?'numbers is an array':'numbers is not an array');
console.log(Array.isArray(isMarried)?'married is an array': 'married is not an array');


